document.querySelectorAll('.btn').forEach(button => {
    button.addEventListener('click', () => {
        alert('Form functionality is not implemented yet.');
    });
});

// Dark mode toggle functionality
const toggleButton = document.getElementById